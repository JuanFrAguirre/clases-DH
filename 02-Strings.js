// Strings
console.clear();

// length
// indexOf
// slice
// trim
// split
// replace

// splice
let nombre = "Juan Aguirre";

// nombre.split("");

// nombre = nombre.split("").splice(4, 1).join("");
// nombre.splice(4, 1);
// nombre = nombre.join("");

console.log(nombre.split(" ")[1].slice(0, 3));
console.log(nombre.slice(5, 8));

// console.log(nombre);
